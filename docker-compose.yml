version: '3.8'

services:
  web:
    build: .
    container_name: pdf-base64-xml
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - FLASK_ENV=production
    restart: unless-stopped
    volumes:
      # Mount for development (optional - comment out for production)
      - .:/app
